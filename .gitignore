# Secrets - niemals committen
.env

# Verschlüsselte Variante darf committed werden
!.env.gpg

# DB-Dumps mit echten Daten — nie committen
backups/

# requirement-analyst Specs — transiente Build-Inputs, nie committen
specs/
